# Scripts

Top-level scripts for installing and launching the full stack (sidecar + desktop app).

Planned:
- `install.sh` — `uv sync` in `sidecar/`, `pnpm install` and `pnpm build` in `app/`
- `start.sh` — spawn the Electron app pointing at the local sidecar

Not yet implemented; will land alongside the Electron app.
